{
  "name": "vunapos",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "postinstall": "cd vunapos_spa && yarn install --frozen-lockfile",
    "dev": "yarn --cwd vunapos_spa dev",
    "build": "yarn --cwd vunapos_spa build"
  }
}
